spring:
  application:
    name: card_delivery
  datasource:
    driverClassName: org.postgresql.Driver
    password: 0095
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        types:
          print:
            warn :false
    show-sql: true
sentry:
  dsn:
  environment: "development"
  traces-sample-rate: 1.0    # 1.0 - barcha requestlarni kuzatadi
  debug: true                # Debug rejimini yoqing (faqat test uchun)'
  in-app-includes: uz.dev.cardprocess
server:
  port: 8081